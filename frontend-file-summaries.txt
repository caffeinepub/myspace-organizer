{
  "frontend/src/App.tsx": "Root application component with tab-based navigation; fixes RoutinesPage and RecordsPage imports to use default exports via lazy loading, wrapping all three lazy pages in Suspense with PageSkeleton fallback.",
  "frontend/src/pages/NotesPage.tsx": "Full notes page with masonry grid, search, label filtering, archive/trash views, and note modal; adds Import Notes (file picker for .json/.txt/.doc/.docx) and Export All Notes (dropdown with TXT/DOC/JSON formats) buttons in the header, wired to noteImport/noteExport utilities with success/error toasts and instant UI refresh.",
  "frontend/src/index.css": "Global stylesheet defining OKLCH color tokens for light/dark modes, app-specific CSS custom properties (font, accent, background image), and component styles. Fixes the build error by replacing the invalid `@apply border-border/50` (opacity modifier unsupported in @apply) with equivalent plain CSS `border-color: oklch(var(--border) / 0.5)` in the `.note-card` rule.",
  "frontend/src/pages/HomePage.tsx": "Home page component that displays a real-time clock and date, a greeting (\"Hi Dev...💛\"), and widgets for streak, quote, and today's routine, plus a quick-add note FAB and footer.",
  "frontend/src/store/appStore.ts": "Zustand store managing theme (defaulting to light), accent color, and font family settings, now including yellow, gold, and golden-yellow OKLCH accent presets alongside the existing ones.",
  "frontend/src/utils/storage.ts": "localStorage utility functions for persisting theme, accent color, and font family settings, with the default theme changed from 'dark' to 'light'.",
  "frontend/src/pages/SettingsPage.tsx": "Settings page with appearance controls; replaces the grid-of-buttons font selector with a `<select>` dropdown listing all 12 font options (Inter, Poppins, Roboto, Open Sans, Montserrat, Lato, Nunito, Playfair Display, Merriweather, Source Sans 3, JetBrains Mono, Castellar) with proper fallback stacks, wired to appStore's setFont and persisted via localStorage.",
  "frontend/src/utils/dateFormatter.ts": "New shared utility module exporting a `formatDateTime` function that accepts a date string, number timestamp, or Date object and returns a formatted string like \"28 Feb 2026, 3:45 AM\" using 12-hour format with AM/PM; handles date-only strings and null/undefined gracefully without throwing.",
  "frontend/src/components/home/StreakWidget.tsx": "Streak widget component showing current streak count and last check-in date+time; now uses `formatDateTime` from the shared utility instead of `date-fns` format for the last check-in display.",
  "frontend/src/pages/StreakPage.tsx": "Streak tracker page showing current streak, check-in button, stats, and calendar; now uses `formatDateTime` from the shared utility instead of `date-fns` format for the last check-in timestamp display.",
  "frontend/src/pages/RecordsPage.tsx": "Records log page (default export) using the existing `useRecords` hook and `Record` type from `../db/schema`; list view truncates body text to 3 lines (`line-clamp-3`), clicking a record opens a full-content overlay with `whitespace-pre-wrap` and attached image display, and the Add Record file input includes `capture=\"environment\"` for mobile camera capture.",
  "frontend/index.html": "HTML entry point for MyOrganizer Pro; adds a second Google Fonts stylesheet link loading Montserrat, Lato, Nunito, Playfair Display, Merriweather, Source Sans 3, JetBrains Mono, Cinzel Decorative, and Cinzel (with font-display=swap) alongside the existing Inter/Poppins/Roboto/Open Sans link.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with 9 new fontFamily entries (inter, montserrat, lato, nunito, playfair, merriweather, sourcesans, jetbrains, castellar) in addition to the existing sans/poppins/roboto/opensans entries; all other config unchanged.",
  "frontend/src/pages/QuotePage.tsx": "Quote & Motivation page; extends the FONT_OPTIONS array with 8 new fonts (Montserrat, Lato, Nunito, Playfair Display, Merriweather, Source Sans 3, JetBrains Mono, Castellar) while preserving all existing options (Inter, Poppins, Roboto, Open Sans, Georgia, serif), and uses a FONT_FAMILY_MAP for correct fallback stacks including Castellar → \"Cinzel Decorative\", Cinzel, serif.",
  "frontend/src/utils/seedData.ts": "Seeds the IndexedDB database on first run using `../db/db`; creates four capitalized labels (Welcome, Work, Personal, Ideas) and three sample notes with matching capitalized label names, plus records, streak, quotes, and routines.",
  "frontend/src/hooks/useNotes.ts": "IndexedDB-backed notes hook with full CRUD, filtering, bulk actions, and label reassignment; adds `emptyTrash` function that permanently deletes all trashed notes from IndexedDB and updates state immediately.",
  "frontend/src/hooks/useLabels.ts": "IndexedDB-backed labels hook using the real `Label` type (numeric `id`) from `../db/schema` and `../db/db`; guards prevent creating, renaming, or deleting any label named \"All\"; `deleteLabel` accepts an optional `onReassign` callback to strip the deleted label from all notes before removal.",
  "frontend/src/components/notes/LabelManager.tsx": "Label management modal that now accepts an `onLabelsChanged` callback prop; calls it after every successful create, rename, or delete operation, passing rename details (old/new name) so the parent can update the active filter instantly.",
  "frontend/src/hooks/useQueries.ts": "Minimal React Query hooks for backend actor interactions; removes the non-existent `ping` call and instead exports `useGetAllLabels` and `useInitializeDefaultLabels` for the ICP backend label endpoints.",
  "frontend/src/hooks/useSpeechRecognition.ts": "Custom React hook wrapping the browser's Web Speech API using local interface types (avoiding missing DOM lib globals) with continuous recognition and interim results; exposes isSupported, isListening, transcript, interimTranscript, error, startListening, stopListening, and resetTranscript.",
  "frontend/src/utils/noteExport.ts": "Client-side utility for exporting notes as TXT, Word-compatible DOC, or JSON; supports export-all and export-selected variants with timestamped filenames, plus the legacy single-note exportNoteAsDoc function. Uses `note.content` (correct schema field) and handles checklist items.",
  "frontend/src/components/notes/NoteModal.tsx": "Note add/edit modal with all existing functionality preserved; adds `capture=\"environment\"` to the hidden file input inside the image note type upload area so mobile users can capture a photo directly from the camera in addition to uploading from gallery/files.",
  "frontend/src/utils/noteImport.ts": "Client-side utility for importing notes from JSON (full restore), TXT (structured or fallback), and DOC/DOCX (text extraction fallback) files; uses correct Note schema fields (`content`, numeric timestamps), filters undefined ids safely, and strips ids before inserting to avoid IndexedDB key conflicts.",
  "frontend/src/utils/routineExport.ts": "Client-side export utility for routine profiles as TXT, DOC, and JSON; updated to include `[Image attached]` annotation in TXT and DOC exports when a routine item has an `imageId`, and includes `imageId` as-is in JSON exports.",
  "frontend/src/utils/routineImport.ts": "Client-side import utility for routine profiles from JSON, TXT, and DOC files; updated `makeRoutineItem` to accept and preserve the optional `imageId` field, with JSON imports preserving `imageId` and TXT/DOC imports leaving it undefined.",
  "frontend/src/components/notes/MultiSelectToolbar.tsx": "Multi-select action toolbar shown when notes are selected; adds an Export Selected dropdown (TXT/DOC/JSON) alongside the existing Archive and Trash actions, accepting a `selectedNotes` prop and calling the export-selected utilities with toast feedback.",
  "frontend/src/pages/RoutinesPage.tsx": "Full routines page preserving all original functionality; adds image upload section to the add/edit modal with two distinct buttons — Gallery (file picker) and Camera (capture=\"environment\") — plus thumbnail preview on routine item cards, image removal, and replace controls.",
  "frontend/src/utils/recordExport.ts": "New client-side export utility for records; exports `exportRecordsAsTxt`, `exportRecordsAsDoc`, and `exportRecordsAsJson` functions that format all record fields with `formatDateTime` dates and generate timestamped filenames (`records_YYYY-MM-DD_HHMM.ext`).",
  "frontend/src/hooks/useRecordImages.ts": "New utility module (not a React hook) for storing record image attachments in a separate `recordImagesById` localStorage key; exports `compressRecordImage` (canvas-based, max 1600px, 0.7 JPEG quality), `saveRecordImage`, `getRecordImage`, and `deleteRecordImage` — all isolated from existing record data.",
  "frontend/src/db/schema.ts": "Database schema interfaces for all app entities; adds optional `imageId?: string` field to `RoutineItem` to store a reference to a localStorage-backed image for routine items.",
  "frontend/src/hooks/useRoutineImages.ts": "New localStorage-backed utility module for storing routine item image attachments; exports `compressRoutineImage` (canvas-based, max 1200px, 0.75 JPEG quality), `saveRoutineImage`, `getRoutineImage`, and `deleteRoutineImage` — all isolated from existing routine data.",
  "frontend/src/hooks/useRoutines.ts": "IndexedDB-backed routines hook with full CRUD, reorder, and toggle-complete; updated `deleteItem` to call `deleteRoutineImage` to clean up any orphaned image blob when a routine item is deleted."
}